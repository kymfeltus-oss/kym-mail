# Testing

Run `npm test` for Vitest unit tests. Current coverage verifies credential structure, environment failure behavior, safe error translation, sensitive logging redaction, Pub/Sub/OIDC validation, Gmail payload normalization and HTML sanitization, compose/attachment input rules, stable scheduled Message-ID payloads, schedule timing/timezone validation (including DST-boundary instants), and per-type Project validation—including invalid names, missing fields, malformed versioned parameters, deduplication, and immutable type behavior. Run `npm run lint`, `npm run typecheck`, `npm run build`, and `npm audit` for static, production, and dependency checks.

Live verification requires the configured Supabase owner, Google OAuth test user, Gmail API, Pub/Sub topic and authenticated push subscription, verified sender aliases, Vercel deployment, and `CRON_SECRET`. Gate 4 adds past/malformed/invalid-recipient rejection, unavailable-sender/reauthorization/provider-failure behavior, atomic overlapping claims, edit/version conflicts, reschedule/cancel/idempotent cancellation, anonymous and cross-owner denial, private attachment survival, duplicate executor invocation, and a short-horizon production send while the browser is closed. The production acceptance message must arrive externally, appear in ordinary Sent, retain thread/Project context, receive a real external reply through normal Gmail synchronization, and be independent of local development. A separate cancelled message must remain undelivered after its former due time. Verify 390 px mobile, tablet, and 1440 px desktop with no fabricated records or metrics. Tests protect behavior and boundaries; they do not substitute for this live journey.

Gate 5 unit coverage adds whitespace/quoted-phrase/duplicate-term normalization, empty/unclosed/excessive queries, supported filter mapping, deterministic deduplication, explainable matched terms, unsafe HTML/URL rejection, missing optional fields, truthful empty results, malformed provider records, and rate-limit translation. Live acceptance additionally requires valid server-only Adzuna credentials and multiple production searches, working pagination/source links, repeated Save idempotency, Project association persistence across refresh, Saved/Archived behavior, anonymous RLS denial, and responsive search/cards/detail/Project controls at 390 px, tablet, and 1440 px. Provider fixtures validate boundaries but cannot earn Gate 5 PASS without real Adzuna records.

Gate 6 unit coverage validates canonical-key uniqueness, date ranges/precision, non-orphan relationships, project alias canonicalization, authority downgrade rejection, edit allowlists, mandatory resolved authority, empty-edit rejection, and explicit unknown dates. Live acceptance additionally queries production Supabase records to verify KF Resume chronology/titles, the resolved UNT degree, Aston Carter/ESI relationship, the single ASC606 Revenue Engine plus alias, supplemental metrics, provenance, owner RLS, anonymous denial, authenticated hard-refresh persistence, and the creation of an immutable history row after an owner edit. The Career Profile editor is checked at 390 px, tablet, and 1440 px without affecting mail, Projects, scheduling, Job Search, or later gates. Job-analysis negative coverage treats malicious instruction text as inert job-description content, rejects ungrounded requirement wording/evidence IDs/unknown enums, distinguishes unsupported closed-world facts from unknown facts, and never promotes CPA Candidate to a completed CPA credential.

Gate 6A unit coverage adds duplicate-candidate rejection, punctuation/case normalization, exact low-risk dual-source confirmation, conflicting date and metric detection, unique material review, strict extraction schema/extra-field rejection, owner-confirmed overwrite protection, and draft-only stale propagation. Production acceptance must inspect the persisted source-hash extractions and candidate counts, resolve one review exception, edit one confirmed normalized fact, refresh, and confirm the numbered version/provenance entry. Direct anonymous reads and mutation endpoints must remain denied. Responsive checks cover 390 px, tablet, and 1440 px with no horizontal overflow.

Gate 6B unit coverage validates job-description extraction, independent responsibility importance, requirement grounding, unknown-enum rejection, prompt-injection inertness, invented-evidence rejection, closed-world `NO_MATCH` versus open-world `UNVERIFIED`, related-concept caps, CPA-candidate partial matching, deterministic WHY/strategy output, and the `weighted-requirement-v2` formula. Live acceptance analyzes a real saved finance/accounting leadership job, confirms persisted requirements and confirmed-authority Gate 6 evidence links, verifies the 12 unresolved review candidates remain excluded, hard-refresh survival, explicit stale behavior without refresh reruns, failed re-analysis preservation, owner RLS, anonymous denial, and 390 px/tablet/1440 px rendering.

Gate 8 unit coverage includes deterministic direct/agency/unknown posting classification, CFO/Controller/Finance Systems targeting, the unsupported-hiring-manager prohibition, malformed-person rejection, wrong-company exclusion, independent verification/relevance, stale and manual-person states, deterministic ranking, duplicate-person provenance, conflict freshness, and the five-person limit. Apollo boundary coverage includes complete current identity/title/company validation, duplicate provider IDs, empty organization results, timeout, authentication/scope, rate-limit, quota, malformed response, stable identities, and registry/environment behavior. Database tests cover owner-only approval/rejection, one approved person, Project consistency, invalid identifiers, anonymous denial, and stale approval invalidation. Live PASS requires a configured encrypted provider key, real production research, evidence inspection, alternative review, approval persistence, Project-path continuity, and responsive/runtime checks. Without live provider research, Gate 8 remains PARTIAL.

Gate 7 coverage includes fabricated employer/title/date/location/metric rejection, CPA Candidate inflation, unsupported or misclassified technology, duplicate skills, related-evidence overclaiming, duplicate projects, extra provider fields, deterministic request fingerprints, responsibility-weight handling, the five persisted strategy groups, concise material diff, token entropy/hash validation, and malformed-token denial. Database acceptance additionally verifies owner-only RLS, evidence-aware stale propagation, current-analysis staleness, approved snapshot immutability, unapproved-share rejection, and revocation. Browser acceptance uses one real analyzed production job and verifies Master approval, strategy, generation, individual review/edit, final approval, hard refresh persistence, anonymous share, PDF/ATS export, and Project-path continuity at 390, 834, and 1440 pixels. Run `npm run gate7:pdf-qa` with the local server-only environment configured; it renders real authoritative content plus a long-bullet stress case, checks Executive/ATS pagination, and writes ignored QA output under `output/pdf`. Render every page to PNG and confirm selectable text with a parser before closeout.

Calendar tests verify 256-bit token shape/hash, proof magic bytes and safe filenames, Cal.com-only URL validation, normalized public input, provider prefill metadata, webhook parsing/HMAC rejection, private-bucket/RLS migration contracts, and all prior Gate 1–8 regression suites. Live acceptance uses a clearly marked no-funds request: public proof submission, `PAYMENT_SUBMITTED`, owner-only proof display, approve/release, Cal.com booking, signed webhook persistence, real Google Calendar event, hard-refresh survival, and a separate rejection. Check `/consult` and `/app/calendar` at 390, 834, and 1440 pixels. Provider booking/calendar success is never mocked for a PASS result.
